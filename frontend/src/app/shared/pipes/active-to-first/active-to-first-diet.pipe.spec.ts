import { ActiveToFirstDietPipe } from './active-to-first-diet.pipe';

describe('ActiveToFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveToFirstDietPipe();
    expect(pipe).toBeTruthy();
  });
});
