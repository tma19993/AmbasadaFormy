import { TrainingStatusPipe } from './training-status.pipe';

describe('TrainingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TrainingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
